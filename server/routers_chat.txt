
  // AI Chat
  chat: router({
    processTransaction: protectedProcedure
      .input(z.object({
        description: z.string(),
        imageBase64: z.string().optional()
      }))
      .mutation(async ({ ctx, input }) => {
        // For now, return a placeholder response
        // This will be enhanced with LLM integration
        return {
          message: "I've received your transaction details. Please provide more information about the amount and category so I can create the transaction.",
          transactionCreated: false
        };
      }),
  }),
